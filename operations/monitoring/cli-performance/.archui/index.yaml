schema_version: 1
uuid: fa000003
submodules:
  test-playbook: fa000004
links:
  - uuid: 1a5b9c3d
    relation: references
    description: The commands measured here — `archui validate` and `archui index
      --fix` — are defined in the CLI module.
