# Canvas Idle — Transitions

| Trigger | Target State |
|---------|-------------|
| Single-click a module node | `node-selected` |
| Double-click a module node | `drilled` |
| Click `[↗]` on a node header | `drilled` |
| Click a stub edge label | `drilled` (to stub target's parent canvas) |
| Keyboard `Cmd+K` | Opens command palette (overlay, does not change state) |
