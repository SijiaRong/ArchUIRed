---
name: Clean Command
description: Specifies the `archui clean <path|uuid> [--apply]` command that removes dangling UUID references from links, submodules maps, and layout.yaml within a module subtree; dry-run by default, destructive only with --apply.
---
